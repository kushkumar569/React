// we create our own function that return a html code. can be return to main as well as app.
// always use first latter of functin is upper case unless it's not work(like react) and also in vite file type is jsx but react it's js.
function Chai(){
    return (
        <h3>Chai is REady</h3>
    )
}

// chai function is export to the app.jsx and main.jsx
export default Chai